{
  "name": "hardhat-project",
  "version": "0.1.0",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "test:coverage": "npx hardhat coverage",
    "deploy": "npx hardhat ignition deploy ./ignition/modules/Cafereum.ts",
    "buy-coffee": "npx hardhat run scripts/buy-coffee.ts",
    "check-contract-balance": "npx hardhat run scripts/check-contract-balance.ts",
    "withdraw-contract-balance": "npx hardhat run scripts/withdraw-contract-balance.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.0.0",
    "dotenv": "^16.5.0",
    "ethers": "^6.14.4",
    "hardhat": "^2.25.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.3.0"
  }
}
